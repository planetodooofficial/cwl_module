<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="cwl_module.list">
      <field name="name">cwl_module list</field>
      <field name="model">cwl_module.cwl_labels</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="cwl_module.action_window">
      <field name="name">cwl_labels window</field>
      <field name="res_model">cwl_module.cwl_labels</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="cwl_module.action_server">
      <field name="name">cwl_labels server</field>
      <field name="model_id" ref="model_cwl_module_cwl_labels"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="cwl_labels" id="cwl_module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="cwl_module.menu_1" parent="cwl_module.menu_root"/>
    <menuitem name="Menu 2" id="cwl_module.menu_2" parent="cwl_module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="cwl_module.menu_1_list" parent="cwl_module.menu_1"
              action="cwl_module.action_window"/>
    <menuitem name="Server to list" id="cwl_labels" parent="cwl_module.menu_2"
              action="cwl_module.action_server"/>
    -->
  </data>
</odoo>